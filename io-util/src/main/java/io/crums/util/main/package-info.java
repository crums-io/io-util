/**
 * CLI utilities. Using <code>picocli</code> nowadays, so some classes
 * (such as {@linkplain io.crums.util.main.MainTemplate MainTemplate}
 * are a bit dated. Others still find use even with picocli.
 */
package io.crums.util.main;