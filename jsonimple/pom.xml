<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
  
    <groupId>io.crums</groupId>
    <artifactId>util</artifactId>
    <version>${revision}</version>
  </parent>
  
  <artifactId>jsonimple</artifactId>
  <version>${revision}</version>
  
  <name>Jsonimple</name>
  
  <description>
    JSON library. A source-compatible, minimally updated fork of simplejson. Changes
    have been kept to a bare minimum so that diffs with the original are meaningful.
    Support for JSON indent, insertion-ordered writing, and some very lightweight
    object modeling goodies.
  </description>
  
</project>
